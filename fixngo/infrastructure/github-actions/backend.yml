name: Backend CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Install
        run: |
          cd fixngo/backend
          npm install
      - name: Run tests
        run: |
          echo "No tests configured"
